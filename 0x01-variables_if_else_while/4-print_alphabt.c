#include<stdio.h>
/**
 * main - Task four
 * Return: 0
 */

int main(void)
{
	char c;

	c = 'z';
	while
		(c <= 'z') {
			if ((c != 'q' && c != 'e') && c <= 'z')
				putchar(c);
			c++;
		}
	putchar('\n');
	return (0);
}
